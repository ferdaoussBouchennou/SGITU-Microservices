apiVersion: v1
kind: Service
metadata:
  name: g4-coordination
  namespace: sgitu-g4
  labels:
    app: g4-coordination
    group: G4
spec:
  type: ClusterIP
  selector:
    app: g4-coordination
  ports:
    - name: http
      port: 8084
      targetPort: 8084
      protocol: TCP
